/* eslint-disable no-unused-vars */

class Source {
  /**
  * Fetch a course plan.
  * @param {Course} course - The course to fetch.
  * @returns {Course} The course with added data.
  */
  async fetchCourse(course) {

  }

  /**
  * Fetch all available courses.
  * @returns {Array} All available courses.
  */
  async fetchCourses() {

  }
}

module.exports = Source;
